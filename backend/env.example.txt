# Server
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000

# MongoDB Atlas (sample format)
MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.example.mongodb.net/medlaw?retryWrites=true&w=majority

# Firebase Admin (use literal \n for line breaks)
FIREBASE_PROJECT_ID=medlaw-app
FIREBASE_CLIENT_EMAIL=your-service-account@medlaw-app.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n<your-private-key>\n-----END PRIVATE KEY-----\n"

# LLM (Person 5)
LLM_PROVIDER=anthropic          # openai | anthropic | stub
LLM_MODEL=claude-3-haiku-20240307
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
ANTHROPIC_API_KEY=your-anthropic-api-key
LLM_TEMPERATURE=0.1
LLM_MAX_TOKENS=1000

# Python RAG service (Person 3)
PYTHON_RAG_URL=http://localhost:5001

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Copy to .env and replace placeholders with real values
